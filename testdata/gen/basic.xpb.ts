// Code generated by xpbc. DO NOT EDIT.

import { Encoder, Decoder } from '@xpb/runtime';

export interface UserData {
  name: string;
  age: number;
  active: boolean;
}

export class User implements UserData {
  name!: string;
  age!: number;
  active!: boolean;

  constructor(data: Partial<UserData> = {}) {
    Object.assign(this, data);
  }

  static encode(msg: UserData): Uint8Array {
    const enc = new Encoder();
    enc.writeString(1, msg.name);
    enc.writeInt32(2, msg.age);
    enc.writeBool(3, msg.active);
    return enc.finish();
  }

  static decode(data: Uint8Array): User {
    const dec = new Decoder(data);
    const msg = new User();
    while (!dec.eof()) {
      const [fieldNum, wireType] = dec.readTag();
      switch (fieldNum) {
        case 1:
          msg.name = dec.readString();
          break;
        case 2:
          msg.age = dec.readInt32();
          break;
        case 3:
          msg.active = dec.readBool();
          break;
        default:
          dec.skip(wireType);
      }
    }
    return msg;
  }

  encode(): Uint8Array {
    return User.encode(this);
  }
}

export interface AddressData {
  city: string;
  country: string;
  postalCode?: string;
}

export class Address implements AddressData {
  city!: string;
  country!: string;
  postalCode?: string;

  constructor(data: Partial<AddressData> = {}) {
    Object.assign(this, data);
  }

  static encode(msg: AddressData): Uint8Array {
    const enc = new Encoder();
    enc.writeString(1, msg.city);
    enc.writeString(2, msg.country);
    if (msg.postalCode !== undefined) {
      enc.writeString(3, msg.postalCode);
    }
    return enc.finish();
  }

  static decode(data: Uint8Array): Address {
    const dec = new Decoder(data);
    const msg = new Address();
    while (!dec.eof()) {
      const [fieldNum, wireType] = dec.readTag();
      switch (fieldNum) {
        case 1:
          msg.city = dec.readString();
          break;
        case 2:
          msg.country = dec.readString();
          break;
        case 3:
          msg.postalCode = dec.readString();
          break;
        default:
          dec.skip(wireType);
      }
    }
    return msg;
  }

  encode(): Uint8Array {
    return Address.encode(this);
  }
}

export interface ProfileData {
  bio: string;
  avatarUrl?: string;
  user: User;
  address?: Address;
}

export class Profile implements ProfileData {
  bio!: string;
  avatarUrl?: string;
  user!: User;
  address?: Address;

  constructor(data: Partial<ProfileData> = {}) {
    Object.assign(this, data);
  }

  static encode(msg: ProfileData): Uint8Array {
    const enc = new Encoder();
    enc.writeString(1, msg.bio);
    if (msg.avatarUrl !== undefined) {
      enc.writeString(2, msg.avatarUrl);
    }
    enc.writeMessage(3, User.encode(msg.user));
    if (msg.address !== undefined) {
      enc.writeMessage(4, Address.encode(msg.address));
    }
    return enc.finish();
  }

  static decode(data: Uint8Array): Profile {
    const dec = new Decoder(data);
    const msg = new Profile();
    while (!dec.eof()) {
      const [fieldNum, wireType] = dec.readTag();
      switch (fieldNum) {
        case 1:
          msg.bio = dec.readString();
          break;
        case 2:
          msg.avatarUrl = dec.readString();
          break;
        case 3:
          msg.user = User.decode(dec.readMessageBytes());
          break;
        case 4:
          msg.address = Address.decode(dec.readMessageBytes());
          break;
        default:
          dec.skip(wireType);
      }
    }
    return msg;
  }

  encode(): Uint8Array {
    return Profile.encode(this);
  }
}

