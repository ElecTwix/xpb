{
  "name": "@xpb/browser-benchmarks",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "esbuild src/xpb-browser.ts --bundle --outfile=dist/xpb-browser.js --format=esm --target=es2020",
    "bench": "npm run build && npx tsx src/bench-playwright.ts"
  },
  "devDependencies": {
    "@types/node": "^24.10.2",
    "esbuild": "^0.20.0",
    "playwright": "^1.40.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  }
}
